$ARGUMENTS is too broad. Narrow progressively until you hit the load-bearing specific.

Pass 1: What domain is this actually about? Eliminate everything else.
Pass 2: Within that domain, what's the real variable? Strip the rest.
Pass 3: What specific instance, decision, or claim carries the weight?

Each pass halves the scope and doubles the resolution. Stop when further narrowing would lose the thing that matters. The target is the smallest formulation that still contains the full problem.

Output: the zeroed-in formulation and what you cut to get there.
