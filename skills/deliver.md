Produce the thing. Not exploration, not options — the deliverable.

$ARGUMENTS

Before output: identify what "done" looks like and which constraints bind. If ambiguity would change the output, ask up to 3 questions — otherwise proceed directly.

Work from real inputs only. No invented context. Separate facts from assumptions — flag uncertainty, don't guess. When options exist, recommend one with tradeoffs.

Produce v1, QC it internally (likely errors, missing cases, edge cases), iterate once, deliver final. The iteration is invisible — show only the finished output.

This is not a discussion.
